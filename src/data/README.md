# src/data —— 自动抓取的数据放这里

现在是空的。将来「基础数据」「市场动态」两张看板的内容会以 JSON 的形式落在这个目录，
由 `.github/workflows/daily-data.yml` 每天定时写入并提交。

约定：

- 这个目录里的文件**由机器人写**，不要手改（手改会被下次抓取覆盖）
- 每个文件带 `updatedAt` 字段，页面上显示「更新于 X」
- 抓取失败时保留上一次的数据，页面照常显示，只是时间戳变旧

`src/posts/` 里你手写的文章和这个目录完全隔离，机器人不会碰。
