# scripts/fetch —— 抓取脚本放这里

现在是空的，给以后的自动抓取预留的位置。

将来每个数据源一个脚本，职责单一：抓 → 整理 → 写一个 `src/data/xxx.json`。

约定：

- 脚本不许抛出未捕获异常导致整个任务失败。某个源挂了就跳过它，保留上次数据
- 写出的 JSON 必须带 `updatedAt` 和 `source`（数据来源名 + 链接），页面上要标出来
- 不抓需要登录的内容，不抓有版权的正文（新闻只留标题 + 链接）

写好之后在 `.github/workflows/daily-data.yml` 里加一行调用它。
